import * as actions from './action'

const initState = {
  user: null,
}

export default function authReducer (state = initState, action) {
  switch (action.type) {
    default:
      return state
  }
}
